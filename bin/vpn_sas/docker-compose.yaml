version: "3.8"
services:
  sing-box:
    image: ghcr.io/sagernet/sing-box
    ports:
      - "1080:1080"
      - "9090:9090"
    container_name: sing-box
    restart: always
    volumes:
      - ./configs:/etc/sing-box
    command: -D /var/lib/sing-box -C /etc/sing-box run
