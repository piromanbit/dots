[Unit]
Description=HTTP proxy for GitHub Copilot (gost → upstream)
After=network.target

[Service]
Type=simple
EnvironmentFile=-%h/.config/gost/copilot-tunnel.env
ExecStart=/usr/bin/gost -L ${GOST_LISTEN} -F ${GOST_FORWARD}
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
